// Placeholder email provider integration (Resend or SendGrid).
// Will be implemented later once env keys are added.

export function sendEmailPlaceholder() {
  throw new Error("Email provider not configured yet.");
}
